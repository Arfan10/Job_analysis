job_analysis:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres-db
      port: 5432
      dbname: Job_Analysis
      user: postgres
      password: "{{ env_var('DB_PASSWORD') }}"
      schema: public